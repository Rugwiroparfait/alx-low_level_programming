#include <stdio.h>

/**
 * main - prints the phrase "Programming is like build"
 * Return 0 if exited properly non-zero otherwise
 */
int main(void)
{
	puts("\"programming is like building a multilingual puzzle");
	return (0);
}
